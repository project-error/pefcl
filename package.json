{
  "name": "pe-financial",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/project-error/pe-financial",
  "author": "projecterror <info@projecterror.dev>",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky install",
    "postinstall": "husky install",
    "generate:locale": "yarn i18next",
    "build": "yarn build:src && yarn build:web",
    "build:src": "cd src && yarn build",
    "build:web": "cd web && yarn build",
    "prerelease": "yarn generate:locale && yarn build && ./scripts/prerelease.sh"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.0.2",
    "@commitlint/config-conventional": "^16.0.0",
    "husky": "^7.0.4",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3"
  },
  "dependencies": {
    "i18next-parser": "^6.0.0"
  }
}
